multipliers:
  # Enables permission-based sell multipliers.
  # IMPORTANT: Keep these multipliers in ascending order. If a player has
  # multiple multiplier permissions, the highest valid multiplier will apply.
  enabled: true
  permissions:
    # Format: 'permission': 'multiplier'
    ancspawners-1-25x: 1.25
    ancspawners-1-50x: 1.50
